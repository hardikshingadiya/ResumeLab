<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Resume" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/background_warm</item>
        <item name="android:statusBarColor">@color/background_warm</item>
        <item name="colorPrimary">@color/primary_slate</item>
        <item name="colorSecondary">@color/secondary_sage</item>
        <item name="colorSurface">@color/surface_card</item>
        <item name="android:colorAccent">@color/primary_slate</item>
    </style>

    <style name="Theme.Resume" parent="Base.Theme.Resume" />
</resources>
